<div class="bs-docs-header" id="content">
  <div class="container">
    <h1><span class="glyphicon glyphicon-comment"></span> Kontakt</h1>
  </div>
</div>

<div class="container bs-docs-container">
	<div class="row">
		<div class="col-md-9" role="main">
			<div class="panel panel-primary">
				<div class="panel-heading">
					<h3>Kontaktformular</h3>
				</div>
				<div class="panel-body">
					<p>Fragen/Anregungen bitte über unser Kontaktformular. Falls wir dich per Telefon kontaktieren sollen, trage die Telefonnummer bitte im Text ein!</p>
					
					<%= simple_form_for @contact, :html => {:class=>"form-horizontal", :role=>"form"} do |f| %>
						<div class="form-group has-feedback <%= 'has-error' if !@contact.errors[:name].empty? %>">
					    	<label for="inputEmail3" class="col-sm-2 control-label">Name:</label>
					    	<div class="col-sm-6">
					      		<%= f.text_field :name, :required => true, :class => "form-control" %>
					      		<% if @contact.errors[:name].empty? %>
					      			<span class="glyphicon glyphicon-ok form-control-feedback"></span>
					      		<% else %>
					      			<span class="glyphicon glyphicon-remove form-control-feedback"></span>
					      			<%= @contact.errors[:name].join(" ")%>
					      		<% end %>
					    	</div>
				  		</div>				

						<div class="form-group has-feedback <%= 'has-error' if !@contact.errors[:email].empty? %>">
					    	<label for="inputEmail3" class="col-sm-2 control-label">EMail-Adresse:</label>
					    	<div class="col-sm-6">
					      		<%= f.text_field :email, type: 'email', :required => true,  :class => "form-control" %>
					      		<% if @contact.errors[:email].empty? %>
					      			<span class="glyphicon glyphicon-ok form-control-feedback"></span>
					      		<% else %>
					      			<span class="glyphicon glyphicon-remove form-control-feedback"></span>
					      			<%= @contact.errors[:email].join(" ")%>
					      		<% end %>					      		
					    	</div>
				  		</div>	

				  		<div class="form-group has-feedback <%= 'has-error' if !@contact.errors[:message].empty? %>">
					    	<label for="inputEmail3" class="col-sm-2 control-label">Nachricht:</label>
					    	<div class="col-sm-8">
					      		<%= f.text_area :message, :required => true, :class => "form-control", :rows => "8" %>
					      		<% if @contact.errors[:message].empty? %>
					      			<span class="glyphicon glyphicon-ok form-control-feedback"></span>
					      		<% else %>
					      			<span class="glyphicon glyphicon-remove form-control-feedback"></span>
					      			<%= @contact.errors[:message].join(" ")%>
					      		<% end %>
					    	</div>
				  		</div>	

					  	<div class="form-group">
					    	<div class="col-sm-offset-2 col-sm-10">
					      		<%= f.submit "Absenden", :class => "btn btn-primary" %>
					    	</div>
					  	</div>
					<% end %>
				</div>
			</div>		
			<div class="panel panel-primary">
				<div class="panel-body">
					<p>Fragen und Anregungen könnt ihr ebenso loswerden, wenn ihr uns eine Mail unter <a href="mailto:schnitzeljagd@bergwerk-fell.de"><u>schnitzeljagd@bergwerk-fell.de</u></a> sendet.</p>
				</div>
			</div>
		</div>
	</div>
</div>

<script>
  enable_top_menu('kontakt');
</script>